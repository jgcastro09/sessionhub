# Session Hub NPM installer

This package is an installation channel for the Session Hub Go binary. During
installation it downloads the matching GitHub Release archive and
`checksums.txt`, validates SHA-256, extracts the executable, and exposes
`sessionhub`.

No AI CLI, provider SDK, account, or credential is installed or configured.
